Llama Stack の Web 検索 (Web Search with Llama Stack)

Llama Stack の標準機能で最も強力なツールの一つが **Web 検索 (web search)** であり、これは link:https://www.tavily.com/[Tavily^] と統合されています。

これにより、エージェントやアプリケーションは、トレーニングデータだけに頼ることなく、インターネット上のソースから最新かつ最高の情報を入手できるようになります。

[TIP]
====
**Tavily とは何ですか？** Tavily は、LLM アプリケーション向けに特別に設計された AI 最適化検索エンジンです。生の HTML を返す一般的な Web 検索 API とは異なり、Tavily はモデルが直接推論できるクリーンで構造化されたコンテンツを返します。無料枠でも、開発やテストに十分な API 呼び出しが可能です。
====

有効にするには Tavily の API キーが**必要**です。link:https://www.tavily.com/[www.tavily.com^] でサインアップして API キーを作成してください。

image::tavily.png[]

警告：このモジュールには **TAVILY_SEARCH_API_KEY** が**必須**です。Tavily にサインアップしてキーを取得したら、以下の手順に進んでください。

正しいベースディレクトリにいることを確認してください。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
cd $HOME/fantaco-redhat-one-2026/
pwd
----

[.console-output]
[source,bash,subs=attributes+]
----
/home/lab-user/fantaco-redhat-one-2026
----

必要に応じて、Python 仮想環境 (venv) を作成します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python -m venv .venv
----

作成が必要かどうかを確認するには、既存の .venv フォルダを探します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
ls .venv
----

以下のレスポンスが表示された場合は、Python venv を作成する必要があります。

[.console-output]
[source,bash,subs=attributes+]
----
ls: cannot access '.venv': No such file or directory
----

環境を設定します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
source .venv/bin/activate 
----

Tavily は LlamaStack ConfigMap の一部として構成されており、環境変数として設定した独自の Tavily キーを使用します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
grep -A 3 "tavily" $HOME/fantaco-redhat-one-2026/llama-stack-scripts/llamastack-configmap.yaml
----

[.console-output]
[source,bash,subs=attributes+]
----
      - provider_id: tavily-search
        provider_type: remote::tavily-search
        config:
          api_key: ${env.TAVILY_SEARCH_API_KEY:=}
          max_results: 3   
----

web-search サブディレクトリに移動します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
cd web-search
pwd
----

依存関係をインストールします。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
pip install -r requirements.txt
----

Tavily から API キーをコピー＆ペーストし、ターミナルの以下の環境変数に追加します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
export TAVILY_SEARCH_API_KEY=
----

ヒント：`export TAVILY_SEARCH_API_KEY=tvly-dev-abcde...` のようになります。

`INFERENCE_MODEL` 環境変数が正しく設定されていることを確認します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
echo $INFERENCE_MODEL
----

[.console-output]
[source,bash,subs=attributes+]
----
vllm/qwen3-14b
----

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 1_list_tools.py
----

[.console-output]
[source,bash,subs=attributes+]
----
==================================================
Registered Toolgroups
==================================================

Toolgroup ID:  builtin::rag
Provider ID:   rag-runtime
--------------------------------------------------

Toolgroup ID:  builtin::websearch
Provider ID:   tavily-search
--------------------------------------------------

Total toolgroups: 2
==================================================
----

=== 最後のスーパーボウルで優勝したのは誰ですか？ (Who won the last Super Bowl?)

Response API を使用して、最後のスーパーボウルで誰が優勝したかを尋ねるコードスニペットを確認してください。

```python
# クライアントを初期化
client = LlamaStackClient(base_url=LLAMA_STACK_BASE_URL)

# Web 検索なしでレスポンスを作成（非ストリーミング）
response = client.responses.create(
    model=INFERENCE_MODEL,
    input="Who won the last Super Bowl?",
    stream=False,
)
```

Web 検索を使用**せず**、モデルのトレーニングデータのみを使用してスクリプトを実行します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 2_no_web_search.py
----

[.console-output]
[source,text,subs=attributes+]
----
The last Super Bowl, **Super Bowl LVIII**, was played on **February 11, 2024**, at Allegiant Stadium in Las Vegas. The **Kansas City Chiefs** defeated the **San Francisco 49ers** with a final score of **25-22**.

**Patrick Mahomes** of the Chiefs was named **Super Bowl MVP**, leading his team to victory with a strong performance. This marked the Chiefs' second Super Bowl win in three years (following their 2020 victory).
----

非確定的な LLM を扱っているため、受け取る回答は上記とは異なる場合があります。通常、回答には 2023 年または 2024 年の勝者が含まれます。

[NOTE]
====
**なぜ回答が古いのですか？** モデルのトレーニングデータにはカットオフ日（Qwen3 の場合は通常 2024 年半ば）があります。トレーニング中に学習した内容に基づいてしか回答できません。そのカットオフ後の出来事（2025 年 2 月の第 59 回スーパーボウルなど）は、外部ツールなしではモデルには見えません。
====

次に、組み込みツールを使用して Tavily Web 検索を使用し、**より最新の回答を受け取れる可能性**を高めます。

以下のスニペットを確認してください。

```python
# Tavily API キーを使用してクライアントを初期化
client = LlamaStackClient(
    base_url=LLAMA_STACK_BASE_URL,
    provider_data={"tavily_search_api_key": TAVILY_SEARCH_API_KEY} if TAVILY_SEARCH_API_KEY else None,
)

# Web 検索を使用してレスポンスを作成（非ストリーミング）
response = client.responses.create(
    model=INFERENCE_MODEL,
    input=QUESTION,
    tools=[{"type": "web_search"}],
    stream=False,
)
```

スクリプトを実行します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
QUESTION="Who won the last Super Bowl?" python 3_web_search.py
----

注：2026 年 2 月 8 日以降にこのクエリを実行している場合は、おそらく異なる回答が返されます。

[TIP]
====
**どのように動作しますか？** `tools=[{"type": "web_search"}]` が指定されると、Llama Stack は回答する前に Tavily を呼び出すようモデルに指示します。Tavily は Web を検索し、関連するスニペットを返し、モデルはその最新の情報を回答に取り込みます。コードでは、Llama Stack が検索サービスで認証できるように、`provider_data` を介して Tavily API キーも渡していることに注目してください。
====

[.console-output]
[source,text,subs=attributes+]
----
Tavily Key: Set
Question:   Who won the last Super Bowl?

The Philadelphia Eagles won the most recent Super Bowl (Super Bowl LIX in 2025), defeating the Kansas City Chiefs with a score of **40-22** in New Orleans. This marked the Eagles' second Super Bowl championship and their first since 2018.
----

=== 現在のアメリカ大統領は誰ですか？ (Who is the current US President?)

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 2_no_web_search_president.py
----

[.console-output]
[source,text,subs=attributes+]
----
As of the latest information available (up to July 2024), **Joe Biden** is the 46th President of the United States. His term began on **January 20, 2021**, following the 2020 presidential election. The next U.S. presidential election is scheduled for **November 5, 2024**, with the new president to be inaugurated on **January 20, 2025**.

If you're asking this question after July 2024, the answer may change depending on the election results. Let me know if you'd like updates on the 2024 race!
----

今度は Web 検索を使用します。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 3_web_search_president.py
----

注：非確定的な動作のため、Web 検索ツールを使用してもジョー・バイデンが大統領であると報告される場合があります。このモジュールの後半で説明するように、**「今日 (Today)」**を提供することで、LLM+Tavily の判断を支援する試みを行っています。

[.console-output]
[source,text,subs=attributes+]
----
The current President of the United States is **Donald John Trump**.
He was sworn into office on **January 20, 2025**, and his term is set
to end on **January 20, 2029**. This information is consistent across
multiple sources, including official government websites and encyclopedic
references.
----

モデルの知識のカットオフ日を確認しておいてください。

=== 知識のカットオフ日 (Knowledge Cutoff Date)

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 4_what_is_my_knowledge_cutoff.py
----

[.console-output]
[source,text,subs=attributes+]
----
My knowledge cutoff is **Q3 2024**, meaning my training data includes
information up to that time. However, I do **not** have real-time internet
access, so I cannot provide updates or information beyond this cutoff.
For the most current data, I recommend checking reliable sources directly.
Let me know if you'd like help with anything within my training scope!
----

[NOTE]
====
**なぜカットオフを確認するのですか？** モデルの知識のカットオフを知ることで、どの質問に Web 検索による拡張が必要で、どの質問にトレーニングデータだけで対応できるかがわかります。カットオフ後の出来事に関する事実に基づいた質問には、Web 検索が不可欠です。安定した知識（数学、科学、歴史）については、通常、モデルのトレーニングデータで十分です。
====

=== 今日 (Today)

検索結果を向上させるための重要なヒントは、すべてのプロンプトに **「今日 (Today)」** を追加することです。

----
today = date.today()
prompt=f"Today is {today}. Who is the current US President?"
----

今日の目的の日付を知るだけで、「現在 (current)」、「第2四半期 (Q2)」、「昨年 (last year)」といった時間的な性質を持つ事柄についてモデルが推論するのに役立ちます。

[TIP]
====
**シンプルだが強力なパターン:** すべてのプロンプトの先頭に `Today is {date}` を追加することは、低コストで効果の高い手法です。これにより、モデルは「現在」（Web 検索が必要）と「過去」（トレーニングデータで十分）を区別できるようになり、Tavily もより関連性の高い、時期に合った結果を返すことができます。本番環境のプロンプトテンプレートの標準的な一部として検討してください。
====

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
python 5_web_search_president_today.py
----

[.console-output]
[source,text,subs=attributes+]
----
As of January 8, 2026, the current U.S. President is **Donald Trump**.
He secured a second term in the 2024 election, as indicated by multiple
sources, including news articles and the official White House website.
----

### 回答 (Responses)

Responses API を介して Web 検索ツールを使用することもできます。

[.console-input]
[source,bash,role="copypaste",subs=attributes+]
----
QUESTION="Who won the last Super Bowl?"

cat << EOF | curl -sS "$LLAMA_STACK_BASE_URL/v1/responses" \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer $API_KEY" \
    -H "X-LlamaStack-Provider-Data: {\"tavily_search_api_key\": \"$TAVILY_SEARCH_API_KEY\"}" \
    -d @- | jq -r '.output[] | select(.type == "message") | .content[0].text'
{
  "model": "$INFERENCE_MODEL",
  "input": "Today is $(date +%Y-%m-%d). $QUESTION",
  "tools": [{"type": "web_search"}],
  "stream": false
}
EOF
----

== まとめ (Summary)

このモジュールでは以下のことを行いました。

* Web 検索の**あり・なし**でモデルの回答を比較し、トレーニングデータのカットオフによって回答が古くなる様子を確認しました。
* Llama Stack の組み込み `web_search` ツールを介して Tavily を使用し、最新情報を取得しました。
* モデルの **知識カットオフ日 (knowledge cutoff date)** を知ることの重要性を学びました。
* 時間的推論と検索の関連性を向上させるために、**"Today is..."** プロンプトパターンを適用しました。
* Python SDK と直接の REST API 呼び出しの両方から Web 検索にアクセスしました。

Web 検索はエージェントに追加できる最もシンプルなツールの一つであり、LLM に対する最も一般的な不満の一つである「情報の古さ」を即座に解決します。
